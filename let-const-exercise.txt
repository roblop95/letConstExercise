Const PI = 3.14;

Var is a global scope variable while let is a block scope variable. Once declared, var can be redeclared but let can't.

Var is global scope variable and const is not. Var can be redeclared and reassigned while const can't.

Let variable can be reassigned while const variable can't.

Hoisting is when variables and functions are taken to the top of the scope which can be used anytime even though they are declared later on.